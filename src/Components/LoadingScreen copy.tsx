

export const LoadingScreen2 = (p: { loading: boolean, direction:string, position:string }) => {
  return (
    <div
      className="flex items-center z-40 top-0 bg-secondary-600 h-full w-full translate-y-full transition-transform duration-500">
    </div>
  );
};